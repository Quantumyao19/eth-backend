apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global: 
      scrape_interval: 5s

    scrape_configs:
      - job_name: "eth-backend"
        metrics_path: "/metrics"
        static_configs:
          - targets: ["eth-backend:8080"]